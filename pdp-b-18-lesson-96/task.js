let arr = [1, 2, 3, 4,5];
let newArr = [];

let newArrLn = 0;
let ln = arr.length;
function javob(a) {
  if (a == 1) {
    return newArrLn;
  }
  newArrLn = newArrLn * a;
  return factorial(a - 1);
}



let count = javob(ln);

function arrays(arr) {
  for (let i = 0; i < count; i++) {
    for(let j=0; i < count; i++){
        a[j] =arr[];
    }
  }
}

kombinatorika(arr);